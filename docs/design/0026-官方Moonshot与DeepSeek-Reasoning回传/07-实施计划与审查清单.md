# 实施计划与代码审查清单

## 实施原则

- 不修改不属于本功能的文件；已有未提交改动视为用户工作，不得重置、格式化或顺带处理。
- 先完成 SDK/endpoint Spike，再合入 Provider 生产路径。不能以手工构造“理想 replay JSON”替代 SDK 转换证据。
- 每个阶段可独立构建、审查和回滚。OpenAI 回归失败时先修公共抽象，不得放宽 OpenAI 校验掩盖问题。
- 新 Provider 的严格能力表 preflight 是最早调用门禁；不得先创建 streaming Assistant 再发现模型不支持。

## 阶段任务与完成定义

| 阶段 | 输入 | 改动范围 | 测试 | 完成定义 |
|---|---|---|---|---|
| Provider discovery/SDK Spike | 候选 `@ai-sdk/*@ai-v5`、官方资料 | mock HTTP/SSE、Provider descriptor、能力记录 | [06](./06-Spike测试与验收.md) Spike | 有 URL、`/v1`、model list/fallback、factory/serializer、精确模型结论；无伪造表项 |
| 严格能力门禁 | Spike 准入结果 | Worker 单一静态 capability 模块、preflight | 未命中无网络/Assistant/ToolExecution | 新 Provider 只有表项可被主调用与 Worker single-call 调用 |
| Settings/options/基础工厂 | capability API、官方 SDK | shared sanitizer/merge、npm union、API/Web 清理、Worker/shared factory | settings/Web/factory/mock URL | 非 reserved 保留、reserved/危险键清理、fixed-last 一致；官方 factory/namespace 正确 |
| 公共 replay 契约 | protocol/version | shared envelope、internal API schema、API source/provenance | shared/API/OpenAI 回归 | union 严格，OpenAI JSON 不变，null 边界正确 |
| Adapter/Runner 生命周期 | types、capability、hook | context、part hook、payload wrap、recovery fence、atomic complete | Worker/API 闭环 | 没有双 namespace、空消息、半提交、recovery identity 泄漏或 attempt 状态串用 |
| Moonshot/DeepSeek 接入 | 精确表项 | 各 Adapter/registry/fixtures | mock + 官方端点 | 每项通过多轮、工具、切换、Fork/Revert/Compaction |
| 发布收尾 | 全证据 | 发布记录、能力表同步、回归 | 全 workspace tests | 只声明 Worker 表中 verified 模型，限制明确 |

## Provider discovery 与能力表步骤

### SDK/服务发现

- 安装/解析 `@ai-sdk/moonshotai@ai-v5`、`@ai-sdk/deepseek@ai-v5`，记录 lockfile、`ai` 和 transitive provider 版本；不升级 `ai` major。
- 用类型和 mock HTTP 确认 factory、chat model selector、provider options namespace、实际模型 ID serializer。
- 确认官方默认 Base URL、SDK 是否自动处理 `/v1`、模型列表 endpoint/认证/shape，以及模型列表失败时 existing configured-model fallback。
- 确认设置页官方文档链接与 Provider 文案。未证实的信息不得变成硬编码默认值。
- 用 fixture 覆盖 reasoning/text/tool chunks、空 text、空/缺失 reasoning、多 block、中断和工具多子轮。

### 能力表落地

- 新建 Worker 内静态 capability 模块，按 `{ providerNpm, exact actual model }` 精确匹配，返回 protocol/version 与仅含 reserved 参数的 `fixedOptions`；不得直接返回含用户 options 的最终 payload。
- 初始表无虚构模型；每次准入同步写入 [06](./06-Spike测试与验收.md) 发布记录。
- 在新 Provider `runModelStep()` 的 model 创建、network、streaming Assistant、工具输入之前执行 preflight；未命中失败，不允许 Registry null 降级。
- Worker single-call 也先 resolve 同一表，并把内部 policy 显式传给 shared single-call；shared 缺 policy 时 fail closed。
- 能力表不暴露给 Web/API；Web 无 replay 状态 UI 或新 capability endpoint。

## Settings、Web 与工厂步骤

### 共享/API/Web

- 扩展 `AgentProviderNpmSchema`、`SingleCallProviderNpm` 及全部穷尽分支。
- 在 shared package 增加唯一 plain-object sanitizer、reserved-key 常量和 fixed-last merge 纯函数；顶层 key 仅以 `trim → 去 '_'/'-' → lowercase` 匹配 reserved，危险键至少处理 `__proto__`、`prototype`、`constructor`。
- sanitizer 递归只保留 JSON plain-object/array/primitive 数据，清理嵌套危险键；不递归删除普通嵌套 option 的 reserved 同名键。
- API 保存、Worker invocation、Web UX 都复用这个 shared 函数；API/Worker 是权威。禁止三份自定义 matcher。
- 对新 Provider model options，在 API 保存路径剥离 reserved 等价键；Worker 读取历史 options 时同样清理；下次保存清理，不报配置冲突。
- Web 不增加 thinking 控件；generic JSON/Options 的帮助、校验、提交清理禁止 reserved keys，并测试无法绕过 API/Worker 固定策略。
- 增加 shared 单测：合法非 reserved 保留、nested thinking 整对象清除、fixed 不被改写、危险键与非 plain object 被过滤、Agent/single-call 同 payload。
- 测试 configured-model fallback 与 model list 不可用，不得要求在线模型发现才能使用已配置 verified 模型。

### Worker/shared factory

- 在 `runner.ts`、`single-call.ts` 以官方工厂创建 Moonshot/DeepSeek chat model；实际模型计算必须统一使用 `providerModelId` 优先规则。
- 固定职责顺序为：`capability.buildProviderOptions()` 生成 fixedOptions → shared `mergeReasoningProviderOptions(rawNamespacePayload, fixedOptions)` 清理并 fixed-last 浅合并 → Agent Adapter 在 `prepareInvocation()` 中调用/使用该逻辑、完整保留 prepared payload，同时处理 replay/context → Runner 只按 `providerOptionsKeyByNpm()` 原样包装一次 namespace。
- Worker single-call 也按 capability fixedOptions → 同一 shared merge → 原样消费 prepared payload 的顺序执行；shared single-call 未获该内部 policy 时 fail closed，且不得自行合并、修改或丢弃 option。
- 为 unsupported npm 保持显式错误；不回退 OpenAI、Anthropic 或 Compatible。
- Single-call 固定开启思考但不读取/写入 history replay；适用于 compaction 等一次性调用。

## 契约、API 与 Runner 步骤

### replay union 和 read-side

- 将 `agent-provider-replay.ts` 改为 strict discriminated union；保留 OpenAI `version: 1` JSON，不增加字段，只在 provenance 投影映射 protocolVersion 1。
- 新 Provider Part item 使用 `text`/`reasoning`/`tool_call`；所有持久化 Part（含空 text）有 metadata。
- API 以整条 Assistant 所有 replay-capable Part 推导 provenance；任一缺失/冲突/unknown/legacy 生成 null 硬边界。
- 有原始 reasoning 但无 visible content 的 completed Assistant 始终有 protected 空占位/source；source null 仍必须保留。
- `assistantOrdinal` 继续表示 provider-neutral messages index；保持名称，不以计数器实现。
- 新 Provider primary projection descriptor 显式 `undefined`/不兼容；不新增 retained-tail Assistant anchor。

### Adapter/Runner

- 所有 Adapter（含 OpenAI）返回 mandatory readonly attemptContext；Runner 只有有 Adapter 时调用 `createAttempt(context)`。
- 增加 part materialization hook，在创建 text/reasoning 与 push tool_call 后调用；`text-start` 无 delta 同样写 metadata。
- `finalizeAttempt()` 的成功值提供 `allowsReplayOnlyAssistant`；Runner 用协议能力、非空 reasoning、identity 一致、final flush 成功共同决定 reasoning-only 完成。
- Adapter 在恢复后删除无法恢复的 API 边界空 Assistant；最终 SDK request 不得有 `content: []` 或空 text。
- Runner 完整保留 prepared payload 中合法 non-reserved 与 fixed options（例如 `{ parallelToolCalls: true, thinking: ..., reasoningHistory: ... }`），不重新合并、不修改、不丢弃，只按 npm namespace 包装一次；增加双 namespace 反断言。
- 以严格顺序实现 `finalize → final flush → 构造 queued inputs → API 原子 complete+create queued → 后续执行工具`。
- 为 recovery continuation 增加同 Run/same identity fence、已有 Part 加载和 identical metadata 核验；existing Part 的缺失/损坏/identity、position、Part ID 冲突 fail closed。不能证明续接时用安全 replacement；replacement 不能隔离旧 Part 时 fail closed。
- hook/invariant 本地异常在 flush/complete 前中止、不重试；provider stream/API 错误沿既有 retry classifier。

## 代码审查清单

### 产品与能力范围

- 是否只走两个官方 SDK，完全没有新 Compatible 路径？
- 是否只有 Worker 精确能力表项可运行，未命中是否在网络前失败？
- 是否不存在“普通调用但不 replay”或 UI capability 暗示？
- 是否没有 thinking UI、disabled、配置冲突或 default reasoning effort？
- Worker single-call 是否复用同一能力表/fixed policy 且没有 history replay？

### 配置、payload 与身份

- API/Web 保存与 Worker 读取是否都剥离/忽略 reserved keys？
- 是否存在 shared 唯一 sanitizer/merge，而非 API/Web/Worker 三套规范化？是否只在顶层删除 reserved、递归清理危险键和非 plain object？
- `capability.buildProviderOptions()` 是否只生成 reserved fixedOptions；是否只有 shared `{ ...sanitizedUserOptions, ...fixedOptions }` fixed-last 浅合并合法、无 reserved 深合并？
- Adapter 是否在 `prepareInvocation()` 调用/使用 shared merge、完整保留合法 non-reserved options 与 fixed options，并处理 replay/context？Agent/single-call payload 是否一致？
- Runner 是否不重新合并、修改或丢弃 prepared 内部 option，只按一次 namespace 包装？`PreparedProviderInvocation.providerOptions` 是否没有 `moonshotai.moonshotai`、`deepseek.deepseek`？
- 所有 Adapter 是否有 mandatory readonly context；retry、工具子轮、下一请求是否无 mutable context 泄漏？
- capability 是否使用精确实际模型 ID，未以 prefix/baseURL/displayName 猜测？

### 数据、回放和空消息

- envelope 是否严格、无正文/凭证/baseURL/tool 参数；OpenAI JSON 是否保持兼容？
- text/reasoning/tool_call（含空 text）是否都带 provenance，并在 Assistant 粒度统一核验？
- legacy/损坏/冲突数据是否形成 null 硬边界？
- reasoning-only legacy/损坏 Assistant 是否有 API 边界占位、却不被发送给 Provider？
- 连续段是否按 messages index/Part.position 稳定恢复，且不跨 identity 边界？
- primary projection 是否明确不支持新 Provider Assistant anchor？

### 事务、失败与安全

- `finalize`/final flush 失败是否绝不调用原子完成、绝不创建 queued ToolExecution？
- 成功路径是否在一个 API 事务中完成 Assistant 和创建 queued execution，工具仅在之后执行？
- hook 异常是否在提交前终止且不 retry；网络/SDK 错误是否沿现有 classifier？
- recovery 是否只允许同 Run/same identity；已有 Part metadata identical 是否幂等，缺失/损坏/冲突/position/Part ID 冲突是否 fail closed？
- recovery/replacement 失败后是否无 completed Assistant、无 ToolExecution、无 PromptContext replay history，且没有猜测续接？
- 是否不存在失败 Attempt replay、metadata 失败后模型重发、400 自动降级或数据库回捞？
- debug/error/log 是否没有 reasoning、body、tool output、replay JSON 或凭证泄露？

## 回滚与条件升级

- 能力表项可独立移除，从而在不改历史 replay 数据的前提下停止某精确模型调用；不得以降级普通调用替代。
- 公共 union 导致 OpenAI 回归时，优先恢复 OpenAI parser/normalizer/update 语义，不能放宽 schema。
- 官方端点拒绝异构历史时，收紧支持说明并停止对应发布；不做自动角色转译。
- 若 Spike 证明标准事件不能表达必要状态，停止本期常规实现，单独提交最小数据需求、迁移/隐私影响和替代方案的条件升级设计。
