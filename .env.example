DATA_DIR=.data
HOST=127.0.0.1
PORT=4310

# 仅前端开发期（Vite dev server）使用
# - DEV_WEB_PORT：前端 dev server 监听端口（例如 5173/5174）
# - DEV_API_ORIGIN：前端 dev proxy 的后端目标（需要代理到远端时再设置）
DEV_WEB_PORT=
DEV_API_ORIGIN=

# Docker/单端口交付（Fastify 托管 Web 静态资源 + SPA history fallback）
# - SERVE_WEB=1 时，后端会把非 /api/* 的页面路由回退到 index.html
SERVE_WEB=0
# 生产环境可将 apps/web/dist 打包进镜像后，设置为对应目录
WEB_DIST_DIR=apps/web/dist

# Credential 主密钥（可选）
# - 为空时会在首次启动时自动生成并落盘到 DATA_DIR/keys/credential-master-key.json
# - 建议在 Docker 交付时显式设置（32 字节 hex/base64/base64url）
CREDENTIAL_MASTER_KEY=

# 访问鉴权（可选）
# - 设置 AUTH_TOKEN 后启用鉴权：需要先在首页输入 token 登录，获取会话 Cookie
# - 未设置 AUTH_TOKEN 则保持无鉴权（适合仅本机使用）
AUTH_TOKEN=
# Cookie Secure 属性开关（仅 HTTPS 场景建议开启；本地 http 开发保持 0）
AUTH_COOKIE_SECURE=0
